# Dependencies
node_modules/

# OS
.DS_Store
**/.DS_Store

# Local env — never commit secrets
.env
.env.local
**/.env
**/.env.local

# Build output
build/
dist/
*.log

# E2E run logs
contract/logs/

# Local prover deployment state — contains an issuer secret
contract/e2e/.deployment-*.json

# Resumable wallet sync checkpoints — contain wallet state, never commit
contract/e2e/.wallet-state-*/
design/DESIGN.md

# Internal design documents and study contracts — declared in PRIOR-WORK.md, kept out of the public repo
design/DESIGN.md
design/EchoForge Midnight.dc.html
examples/
video